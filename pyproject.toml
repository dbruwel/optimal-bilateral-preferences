[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "optimal_bilateral_preferences"
version = "0.1"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
